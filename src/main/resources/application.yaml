ktor:
  application:
    modules:
      - com.financial.ApplicationKt.module
  deployment:
    port: 8080
jwt:
  secret: "your-secret-key-change-this-in-production-min-256-bits"
  issuer: "http://localhost:8080/"
  audience: "financial-app-users"
  realm: "FinancialApp"
  expirationTime: 604800000
  refreshTokenExpiration: 2592000000


database:
  host: "localhost"
  port: 5432
  name: "financial_db_dev"
  user: "root"
  password: "123456"
  maxPoolSize: 10
  driver: "org.postgresql.Driver"
  jdbcUrl: "jdbc:postgresql://${database.host}:${database.port}/${database.name}"

minio:
  endpoint: "http://localhost:9000"
  accessKey: "minioadmin"
  secretKey: "minioadmin123"
  bucketName: "financial-app"
  publicUrl: "http://localhost:9000/financial-app"

email:
  smtp:
    host: "smtp.gmail.com"
    port: 465
  username: "your-email@gmail.com"  # Change this to your Gmail
  password: "your-app-password"      # Use Gmail App Password
  from: "noreply@financial.app"
  fromName: "Financial App"

cors:
  allowedHosts: "localhost:3000,localhost:8080,127.0.0.1:3000"

app:
  environment: "development"
  debugMode: true
  frontendUrl: "http://localhost:3000"  # Frontend URL for reset password link
